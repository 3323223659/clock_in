<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.clock_in.mapper.CourseMapper">
    <resultMap type="com.ruoyi.clock_in.domain.Course" id="CourseResult">
        <result property="id"    column="id"    />
        <result property="courseName"    column="course_name"    />
        <result property="teacherName"    column="teacher_name"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateTime"    column="update_time"    />
        <result property="isDelete"    column="is_delete"    />
    </resultMap>

    <select id="selectNameTeacher" resultMap="CourseResult">
        select * from clock_in_course where course_name = #{name} and teacher_name = #{teacherName} and  is_delete = 0
    </select>

    <!-- 保存 Course 对象 -->
    <insert id="insertCourse" parameterType="com.ruoyi.clock_in.domain.Course">
        INSERT INTO clock_in_course (course_name,
                                     teacher_name,
                                     is_delete,
                                     create_time,
                                     update_time)
        VALUES (#{courseName},
                #{teacherName},
                #{isDelete},
                #{createTime},
                #{updateTime})
    </insert>
</mapper>